<!DOCTYPE html>
<html>
  <head>
    <title>My AI Generated Art Website</title>
    <style>
      .banner {
        height: 400px;
        background-image: url('https://drive.google.com/uc?export=view&id=1B6fLqd-0Z5o-5c_6YEXTIbrNn1g-OsFf');
        background-size: cover;
        background-position: center;
        display: flex;
        justify-content: center;
        align-items: flex-end;
        max-width: 100%;
        max-height: 100%;
        position: relative;
      }

      .banner-heading {
        font-size: 18px;
        color: #000000;
        opacity: 0;
        position: absolute;
        left: 82%;
        top: 20%;
        transform: translateX(-50%);
        animation: text-fade-in 2.5s forwards 0.8s;
      }

      .mobile-heading {
        display: none;
      }

      @keyframes text-fade-in {
        0% {
          opacity: 0;
        }
        100% {
          opacity: 1;
        }
      }

      @media (max-width: 480px) and (max-height: 240px) {
        .banner {
          height: 280px;
          width: 480px;
          background-size: cover;
          position: relative;
        }

        .banner-heading {
          display: none;
        }

        .mobile-heading {
          font-size: 12px;
          color: #000000;
          opacity: 0;
          position: absolute;
          bottom: 10%;
          left: 80%;
          transform: translateX(-50%);
          animation: text-fade-in 2.5s forwards 0.8s;
          display: block;
        }
      }
    </style>
  </head>
  <body>
    <div class="banner">
      <h1 class="banner-heading">On one particularly stormy night, as the Flying Dutchman traversed the inky-black waves, a fierce storm broke out, unlike any the cursed crew had seen before. The wind howled like a tortured soul, and the ocean roared, churning up colossal waves that threatened to consume the ship.</h1>
      <h1 class="mobile-heading">AI Generated Art</h1>
    </div>

    <script>
      function checkImage() {
        var img = document.querySelector(".banner");
        var imgSrc = img.style.backgroundImage.slice(5, -2);

        var imgObj = new Image();
        imgObj.onload = function () {
          img.style.backgroundImage = `url(${imgSrc})`;
        };
        imgObj.src = imgSrc;
      }

      setInterval(checkImage, 60000); // check every minute
    </script>
  </body>
</html>
